# DEV tanpa backend → mock 2 sampel AW3 (jangan diset).
# DEV lawan API lokal → VITE_API_URL=http://127.0.0.1:3000
# PROD (Hono serve dist, satu domain) → kosongkan.
# PROD (domain terpisah) → https://hr.ksp-nextcloud.my.id
# Panel "Akses cepat — 3 peran" di /login (akun demo seed).
# Otomatis tampil saat DEV / mock. Di build produksi tersembunyi,
# kecuali diset eksplisit =1 (mis. staging UAT).
# VITE_SHOW_DEMO_LOGIN=1
VITE_API_URL=
